version: '3.3'

services:

  data_api:
    build: ./data_api/
    image: stackdemo_data_api
    expose:
      - 8801
    env_file:
      - models.env


  nginx:
    build: ./nginx/
    image: stackdemo_nginx
    ports:
      - "80:80"
