version: '3.3'

services:

  data_api:
    image: 127.0.0.1:5000/stackdemo
    build: .
    hostname: data_api
    container_name: data_api
    expose:
      - 8801

  nginx:
    image: 127.0.0.1:5000/stackdemo
    build: .
    hostname: nginx
    container_name: nginx
    ports:
      - "80:80"
